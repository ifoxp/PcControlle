# PC Control — залежності
flask
python-dotenv
pyautogui
pynput
pycaw
comtypes
opencv-python
numpy
requests
PySide6

# --- Безпека / мережа (крок 1) ---
waitress          # продакшн-WSGI сервер (замість app.run для доступу з інтернету)
cryptography      # генерація self-signed TLS-сертифіката
qrcode[pil]       # QR-код парування на дашборді
pillow            # ImageGrab (потокобезпечний скріншот) + QR-рендер
screen_brightness_control  # яскравість моніторів (DDC/CI)
wmi
